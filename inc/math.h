//
// Created by Wojcik98 on 30.09.18.
//

#ifndef TRZEWIKODZIOB_MATH_H
#define TRZEWIKODZIOB_MATH_H

#define abs(x) (((x) > 0) ? (x) : (-(x)))
#define sq(x) ((x) * (x))
#define min(x, y) (((x) < (y)) ? (x) : (y))
#define max(x, y) (((x) > (y)) ? (x) : (y))

#endif //TRZEWIKODZIOB_MATH_H
